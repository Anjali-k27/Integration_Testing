# CI environment — no real secrets. Matches tests/fakes.py exactly.
COMPOSE_PROJECT_NAME=epoch-cluster

JWT_SECRET=epoch-demo-secret-rotate-via-kms-in-prod
LITELLM_URL=http://litellm:4000
LITELLM_MASTER_KEY=sk-epoch-demo-master-key
